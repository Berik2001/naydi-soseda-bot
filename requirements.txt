aiogram>=3.29.1,<4.0
asyncpg>=0.29
python-dotenv>=1.0
# Опциональное FSM-хранилище: используется только если задан REDIS_URL
redis>=5.0
# Трекинг ошибок: активируется только если задан SENTRY_DSN
sentry-sdk>=2.0
